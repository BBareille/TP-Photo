{% extends 'base.html.twig' %}

{% block title %}Photo{% endblock %}

{% block body %}
    <h1>Photo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photo.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ photo.label }}</td>
            </tr>
            <tr>
                <img src="/uploads/photo/{{ photo.name }}"/>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_image_index') }}">back to list</a>

    <a href="{{ path('app_image_edit', {'id': photo.id}) }}">edit</a>

    {{ include('image/_delete_form.html.twig') }}
{% endblock %}
